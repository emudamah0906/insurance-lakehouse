# Extra packages installed into the Airflow Docker image via DAG pip_install or
# mounted at container start.  Keep in sync with the Airflow image version (2.9.3).

apache-airflow-providers-amazon==8.21.0
apache-airflow-providers-slack==8.6.0
boto3==1.34.101
